apiVersion: v1
kind: Service
metadata:
  name: sample-api
  namespace: apps-dev
spec:
  type: ClusterIP
  selector:
    app: sample-api
  ports:
    - name: http
      port: 80
      targetPort: 8080

